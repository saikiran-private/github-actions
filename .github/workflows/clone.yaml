name: Clone the repo
on:
  push:
jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run: git clone https://github.com/saikiran-private/github-actions.git && cd github-actions && ls -lrt
